function addNUmbers(number1, number2){
    var addition = number1 + number2 
    return addition
}
var result = addNUmbers(20,3)
//we do this because the return statement just returns "addition" but doesn't dp anything to it. so we put it in a variable and display the value of that variable
console.log(result)